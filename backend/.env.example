# Secrets — copy to .env and fill in values
DATABASE_URL=postgres://fantasy:fantasy@localhost:5432/fantasy?sslmode=disable
REDIS_URL=redis://localhost:6379

WCF_AUTH_TOKEN=
UCLF_AUTH_TOKEN=
ODDS_API_KEY=
SYNC_ENDPOINT_SECRET=change-me

# Non-secret config lives in config.yaml (same directory). Override path via CONFIG_PATH if needed.
